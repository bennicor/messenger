package com.example.messenger.voice;

public enum VoiceSignalType {
    JOIN,
    LEAVE,
    OFFER,
    ANSWER,
    ICE_CANDIDATE,
    CALL_INVITE,
    CALL_DECLINE,
    CALL_ENDED,
    CALL_STATE_REQUEST,
    CALL_STATE_RESPONSE
}